#pragma once

#include "forwardcmd.h"
#include "forwardobj.pb-c.h"
#include "tlvbox.h"
/*

static inline ForwardObject* serialize_object_alloc()
{
	assert(0 && "discard function, change your code to tlvbox");
	return NULL;
}

static inline ForwardObject* serialize_object_frombuff(const void *buff, size_t len)
{
	assert(0 && "discard function, change your code to tlvbox");
	return NULL;
}

static void* serialize_object_tobuff(ForwardObject *obj, size_t *len, int padlen)
{
	assert(0 && "discard function, change your code to tlvbox");
	return NULL;
}

static void serialize_object_free(ForwardObject *obj)
{
	assert(0 && "discard function, change your code to tlvbox");
}
*/
